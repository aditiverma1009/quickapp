const axios = require('axios');

const defaultValue = {
 
};

const noteReducer = (prevState = defaultValue, action) => {
  switch (action.type) {    

  

    default: {
  
    }

  }
};
export default noteReducer;

